<script lang="ts">
	import { DropdownMenu, type DropdownMenuSubContentProps } from 'bits-ui'
	import { cn } from '$/lib/utils'

	export type Props = DropdownMenuSubContentProps & {
		class?: string
	}

	let { class: className, children, ...props }: Props = $props()

	const classes = $derived(
		cn(
			'data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 relative z-50 min-w-[8rem] overflow-hidden rounded-md border border-gray-200 bg-white p-1 text-gray-900 shadow-md',
			className
		)
	)
</script>

<DropdownMenu.SubContent {...props} class={classes}>
	{#if children}
		{@render children()}
	{/if}
</DropdownMenu.SubContent>
